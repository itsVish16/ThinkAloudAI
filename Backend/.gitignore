# Secrets — never commit the unified stack env (only .env.example is tracked).
.env

# Local infra (already covered by named volumes, but keep stray data out).
*.db
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
ENV/
env/
